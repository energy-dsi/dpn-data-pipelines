apiVersion: apps/v1
kind: Deployment
metadata:
  name: dpn-airflow-webserver
  labels:
    app: dpn-airflow-webserver

spec:
  selector:
    matchLabels:
      app: dpn-airflow-webserver

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
      
  template:
    metadata:
      labels:
        app: dpn-airflow-webserver

    spec:
      containers:
        - name: dpn-airflow-webserver

          image: "{{ .Values.airflow.image.repository }}:{{ .Values.airflow.image.tag }}"
          imagePullPolicy: {{ .Values.airflow.image.pullPolicy }}

          command: ["airflow", "webserver"]

          ports:
            - containerPort: 8080

          envFrom:
            - configMapRef:
                name: dpn-airflow-config
            - secretRef:
                name: dpn-airflow-secrets      

          env:
            - name: AIRFLOW__DATABASE__SQL_ALCHEMY_CONN
              value: "postgresql+psycopg2://{{ .Values.postgres.username }}:$(POSTGRES_PASSWORD)@$(AIRFLOW__DATABASE__HOST):$(AIRFLOW__DATABASE__PORT)/{{ .Values.postgres.db }}"

            - name: AIRFLOW__CELERY__RESULT_BACKEND
              value: "db+postgresql://{{ .Values.postgres.username }}:$(POSTGRES_PASSWORD)@$(AIRFLOW__DATABASE__HOST):$(AIRFLOW__DATABASE__PORT)/{{ .Values.postgres.db }}"

            - name: AIRFLOW__CORE__FERNET_KEY
              valueFrom:
                secretKeyRef:
                  name: dpn-airflow-secrets
                  key: FERNET_KEY

            - name: AIRFLOW__WEBSERVER__SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: dpn-airflow-secrets
                  key: WEB_SECRET_KEY

          volumeMounts:
          {{- range $path, $_ := .Files.Glob "dags/*.py" }}
            - name: dpn-airflow-dags
              mountPath: "/opt/airflow/dags/{{ base $path }}"
              subPath: "{{ base $path }}"
          {{- end }}

            - name: airflow-secrets-store
              mountPath: /mnt/secrets
              readOnly: true

          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "2Gi"

          securityContext:
            runAsUser: 50000
            runAsGroup: 50000
            runAsNonRoot: true
            allowPrivilegeEscalation: false

      volumes:
        - name: dpn-airflow-dags
          configMap:
            name: dpn-airflow-dags

        - name: airflow-secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: dpn-airflow-kv-secrets

      automountServiceAccountToken: false

---
apiVersion: v1
kind: Service
metadata:
  name: dpn-airflow-webserver

spec:
  type: ClusterIP
  selector:
    app: dpn-airflow-webserver

  ports:
    - name: http
      port: 8080
      targetPort: 8080
